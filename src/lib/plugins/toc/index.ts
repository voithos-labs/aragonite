// TOC plugin — public entry. `tocPlugin()` teaches the editor the `[[toc]]` leaf
// that renders the document's heading list; `TOC_BLOCK` names its kind.
export { tocPlugin, TOC_BLOCK } from './toc-plugin';
