// Emoji plugin — public entry. `emojiPlugin()` teaches the editor GitHub
// `:shortcode:` emoji as atomic glyph widgets on the bare `:` trigger. `EMOJI_TABLE`
// is the generated shortcode → glyph map the recognizer resolves against.
export { emojiPlugin } from './emoji-plugin';
export { EMOJI_KIND } from './emoji-recognizer';
export { EMOJI_TABLE } from './emoji-table';
