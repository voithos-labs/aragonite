# Feature: Keyboard Navigation — Geometry-Based Traversal

Cross-block focus driven by geometry checks (visual line position) rather than logical caret position.

## Geometry-based traversal

- ArrowUp on first visual line of a block moves to previous block
- ArrowDown on last visual line of a block moves to next block
