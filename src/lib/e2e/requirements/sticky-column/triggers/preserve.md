# Feature: Sticky Column, Preserve Triggers

User actions that leave the captured sticky column intact. Vertical arrows (plain or shifted) and IME composition preserve.

## Preserve triggers

- Shift+ArrowDown (selection extension) does not reset the sticky column: a plain ArrowDown after it still lands at the column captured first
