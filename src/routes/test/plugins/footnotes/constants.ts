/** The footnote-definition block kind name — a plain string so the numbering scan
 *  can skip definition blocks without importing the branded declaration. */
export const FOOTNOTE_DEF_KIND = 'footnote-def';
