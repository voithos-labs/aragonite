.DS_Store
node_modules
/build
/.svelte-kit
/package
dist/
*.tgz
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.idea/
tmp/

# Playwright
test-results/
playwright-report/

# Note-taking simulation capture artifacts (screenshots + manifest), kept
# outside test-results/ so Playwright doesn't wipe them between runs.
simulation-captures/

# svelte-package emits a .d.mts beside any scripts/*.mjs that a compiled
# src/lib/test file imports (pack-manifest.mjs, shared with verify-pack). Generated
# build byproduct, pruned from the tarball — not source.
scripts/*.d.mts

# Raw perf harness output; curated numbers live in baseline.json
perf-results/

# Raw conformance sweep output; the curated divergence set lives in baseline.json
conformance-results/

# Personal agent workflow
CLAUDE.md
.claude/
/superpowers/
docs/superpowers/
.superpowers/

# Owner-private documents: the forward plan and the release runbook
docs/roadmap.md
docs/contributing/releasing.md
