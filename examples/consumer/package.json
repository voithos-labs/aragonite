{
	"name": "aragonite-consumer",
	"private": true,
	"type": "module",
	"scripts": {
		"sync-plugins": "node ../../scripts/sync-consumer-plugins.mjs",
		"predev": "npm run sync-plugins",
		"prebuild": "npm run sync-plugins",
		"precheck": "npm run sync-plugins",
		"pretest": "npm run sync-plugins",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview --port 4180 --strictPort",
		"test": "playwright test"
	},
	"devDependencies": {
		"@playwright/test": "^1.60.0",
		"@sveltejs/adapter-node": "^5.0.0",
		"@sveltejs/kit": "^2.55.0",
		"@sveltejs/vite-plugin-svelte": "^5.0.0",
		"katex": "^0.17.0",
		"svelte": "^5.29.0",
		"svelte-check": "^4.0.0",
		"typescript": "~5.6.2",
		"vite": "^6.0.3"
	},
	"dependencies": {
		"@voithos-labs/aragonite": "file:../.."
	},
	"overrides": {
		"cookie": "^0.7.2"
	}
}
