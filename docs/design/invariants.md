# Editor Invariants — Catalog & Enforcement

## What this is

Most rules in a codebase are preferences. A handful of the ones here will cost somebody their file, and this is the list of the second kind.

Raw Markdown round-trips byte-for-byte. A block's DOM text equals its ambient prefix plus its raw bytes. Every block kind has a descriptor. Break one of those and nothing shouts at you. The damage surfaces three layers downstream, in a component that did nothing wrong, hours after the commit that caused it, and by then good luck.

So every load-bearing rule gets a **G-number**, a place where it is checked, and something that fails when it breaks. Not for ceremony. The whole point is that a violation fails **loudly** (you see it) and **locally** (at the seam that broke it, not the poor seam that noticed).

This matters more than it used to: the plugin API freezes at 1.0, and other people's code is about to bind to these.

## The enforcement ladder

**Unrepresentable > guarded > documented.** Every contract climbs as high as it can:

| Rung                | Means                                              | You'll find it in               |
| ------------------- | -------------------------------------------------- | ------------------------------- |
| **Unrepresentable** | The compiler rejects the violation. Nothing to run | Group 3 — type guards           |
| **Guarded**         | A check fires at the seam, in dev or in CI         | Groups 1, 2, 4                  |
| **Documented**      | Prose plus per-instance regression tests           | The `D` code (rare, on purpose) |

Prose is the bottom rung and the last resort. When you touch a rule, ask whether it can climb one.

## How a guard is built

The unifying shape is **one predicate, two consumers** — never the same logic written twice:

```
              ┌──────────────────────────────┐
              │  predicate                   │   pure, no side effects:
              │  src/lib/invariants/*.ts     │   node → violation | null
              └──────────────┬───────────────┘
                  ┌──────────┴──────────┐
                  ▼                     ▼
        runtime DEV assertion      property / negative test
        at the breaking seam       (imports the predicate directly)
```

The runtime side goes through `assertInvariant`, which is dev-only and **never throws** — a false positive must not crash a real editor — and tree-shakes to nothing in production, so the assertions cost zero shipped bytes. Per-commit checks are scoped to the touched subtree, never the whole document, so they stay safe to run inside the large-document workflow the project targets.

**Where the shape doesn't hold, and why.** A minority of runtime guards are inline closures at their own seam rather than shared predicates — G1.15, G1.19–G1.22, and the interaction legs of G1.26. What they check is not a CST node's shape but a **transient in-flight ceremony value**: a prepared commit scope, an unshare chain, an owned table view, an in-flight reveal. Those exist only mid-commit, mid-parse, or mid-gesture; there is no stable object to hand a pure predicate, and no way for a test to reconstruct the exact state independently. They are guarded through the ceremony that produces them (G1.15 by a negative-fixture suite driving `parse()`; G1.26's settle-window leg by driving the real interaction factory into the illegal state — its fold-without-reveal and mutation-with-reveal legs have no test-reachable driver and are netted by the e2e invariant watcher, which is where the mutation leg was confirmed to fire: bypassing the fold in `runCommand` reds the reveal-command specs on `command-during-reveal`) or through the `[aragonite:invariant:…]` console channel that every e2e spec fails on (G1.19–G1.22).

The rule of thumb: **if a test can construct the subject, the predicate is shared.** If only the machinery can, the guard lives where the machinery is.

## Reading the catalog

**Enforcement codes:** `A` runtime DEV assertion · `P` property test (fast-check) · `N` negative fixture · `T` compile-time type guard · `L` lint/source-scan · `D` doc.

Predicates live in `src/lib/invariants/`; property tests and arbitraries in `src/lib/test/invariants/` (`npm run test:editor:invariants`). The Group 2 predicates are **test-only** and must never be exported through `src/lib/index.ts`. The rule underneath that is the checkable one: **no `invariants/` module takes a runtime dependency on `selection/`**. (The barrel already reaches `selection/` transitively through the component tree, so "exporting them would add the edge" was never the reason; keeping the predicate directory off the selection model is.) All phases have landed. Tables reference files, never line numbers.

## Group 1 — Runtime-checkable (A·P·N)

Three seam families run these:

- **Commit primitive** — `invariants/install.ts` `assertCommittedNodes`, invoked from `editor-actions/commit/undo-controller.ts` after each commit's raw rebuild.
- **Bootstrap** — the registration-check flush (`schema/registration-checks.ts`). The first flush (Editor mount, via `runStartupInvariantChecks`) sweeps the live registry — built-ins plus any plugin kind registered pre-mount — for the descriptor-bearing checks (keymap, reservedChrome), while registry completeness stays built-in-scoped (a plugin kind's component may register on its own schedule). Later registrations are validated at the next mount or grammar read (`getOrderedOpeners`), never mid-registration-batch, so intra-batch forward references stay warn-free.
- **Own seam** — the ceremony guards (G1.19–G1.23), the inline-machine transition guards (G1.25–G1.27), the selection-endpoint guard (G1.29), the split-landing guard (G1.34), the single-node-sink guard (G1.35), and the structural-descriptor guard (G1.36), each firing inside the machinery it protects; the landable-caret guard (G1.33) is the one exception, firing at the editor root's focus funnel ABOVE every door it guards, which is what lets a consumer's own caret door inherit it.

| ID    | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Codes   | Predicate · Seam · Test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1.1  | Container raw not stale: `strip(raw) === serialize(children)` (non-strip containers exempt — grid outright, opaque via G1.12–G1.13)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A·P·N·D | `node-shape.ts` `checkStaleRaw` · commit primitive · `stale-raw.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                |
| G1.2  | Registry completeness: every `BlockKind` → descriptor + component                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | A·P     | `registry.ts` `checkRegistryCompleteness` · bootstrap (+ BlockHost visible-raw fallback for `listItem`) · `registry.test.ts`                                                                                                                                                                                                                                                                                                                                                                            |
| G1.3  | _Retired upward_ — `isContainer` iff `rebuildRaw`. The grouped registration shape (G3.6) derives `isContainer` from the `container` group, whose `rebuildRaw` is required, so the pairing violation is unrepresentable; the runtime guard and its tests are deleted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | T       | superseded by G3.6 (`BlockKindRegistration`)                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| G1.4  | No container `setContext`s `HISTORY_KEY`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A·L·N   | `context-keys.ts` `checkNoContainerHistoryKey` · `editor-actions/nested/nested-actions.ts` `setNestedActionsContexts` · `context-keys.test.ts` · `lint/no-container-history-key.test.ts`                                                                                                                                                                                                                                                                                                                |
| G1.5  | Category↔field legality (leaf/container fields; a wrap-less container carries no innerPrefix)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | A·P·N   | `node-shape.ts` `checkCategoryFields` · commit primitive · `category-fields.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                    |
| G1.6  | `cloneMetadata` clone-safety                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A·P     | `node-shape.ts` `checkCloneSafeMetadata` · both copy seams: `tree-operations/clone.ts` `cloneNode` and `tree-operations/unshare.ts` `copyNode` · `clone-safe-metadata.test.ts`                                                                                                                                                                                                                                                                                                                          |
| G1.7  | Metadata-driven-raw writes go through `updateBlockMetadata`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | A·N     | no predicate of its own — enforced by routing · `editor-actions/block-edit-core.ts` `updateBlockMetadata` rebuilds raw after the merge (both factories delegate to the shared core) · `editor-actions/update-block-metadata.test.ts` (G1.1 stale-raw is the runtime backstop for bypasses)                                                                                                                                                                                                              |
| G1.8  | `getContentRange` well-formed for every kind that has one — prose kinds, plus a non-prose kind declaring its own (the directive leaf), since the range is consumed without asking `supportsInline`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A·P·N   | `descriptor.ts` `checkContentRange` · commit primitive · `descriptor.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                           |
| G1.9  | Snapshot aliasing (bytes-scoped): no mutation writes serialized bytes through a node an undo/redo entry shares; exempt: live-tree moves of shared nodes (the inline cache is an external WeakMap, so node writes never touch it). Internal readers and the public plugin surface hold bytes-readonly views (`core/node-views.ts`), so a reader-side byte write is a compile error — G4.13 holds the door; the DEV integrity oracle stays (runtime JS bypasses types)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | T·A·P·N | `snapshot-integrity.ts` `checkSnapshotIntegrity` · commit primitive (top undo entry) + undo/redo restore (`editor-actions/commit/history.ts`) · `snapshot-integrity.test.ts`, `test/undo/undo-restoration.property.test.ts`                                                                                                                                                                                                                                                                             |
| G1.10 | Opener-registry coherence: every opener's kind has a descriptor; priorities unique                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A·N     | `registry.ts` `checkOpenerRegistry` · bootstrap · `opener-registry.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                             |
| G1.11 | Keymap coherence: every keymap chord is well-formed (Mod/Alt/Shift + non-empty key) and names a known command id; chords unique per kind                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A·N     | `registry.ts` `checkKeymapCoherence` · bootstrap · `keymap-coherence.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                           |
| G1.12 | Opaque container raw not stale: raw reparses to children byte-matching the live children (chrome compared positionally for `reservedChrome` declarers; a kind with a standalone recognizer whose raw no longer reparses to its own kind fires, and a kind with none bails; a directive kind counts as recognized, so the arm fires for one)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | A·N     | `node-shape.ts` `checkOpaqueStaleRaw` · commit primitive · `opaque-contract.test.ts`, `opaque-stale-raw-directive.test.ts`                                                                                                                                                                                                                                                                                                                                                                              |
| G1.13 | Opaque rebuild determinism: `rebuildRaw` is deterministic over committed state (probe-vs-probe)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | A·N     | `node-shape.ts` `checkOpaqueRebuildDeterminism` · commit primitive · `opaque-rebuild-determinism.test.ts`                                                                                                                                                                                                                                                                                                                                                                                               |
| G1.14 | Reserved-chrome slot: a container declaring `reservedChrome` holds a chrome leaf of the declared kind at child 0 (exempt: kinds with no declaration)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | A·N     | `node-shape.ts` `checkReservedChromeSlot` · commit primitive · `reserved-chrome-slot.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                           |
| G1.15 | Opener return trust (at parse): a plugin opener claims at least one line (`consumed >= 1`) — a return claiming none is **declined in every build**, so the line falls through to the next opener and ultimately the paragraph fallback instead of spinning the parse loop, and `opener-advance` warns naming the kind and the decline; its `node.raw` byte-matches the consumed source lines (else `opener-raw` warns — serialize reads `raw` only, so drift silently breaks round-trip)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A·N     | inline guards, module-private to `core/parser.ts` (no importable predicate — the test drives them through `parse()`) · parser dispatch, per-block: the decline is unconditional, the warn and the raw check are DEV-gated · `parser-opener-guards.test.ts`                                                                                                                                                                                                                                              |
| G1.16 | Commit path dialect: every coordinate a commit declares (`op.eventPath`, `snapshot.path`) is doc-absolute — each prefix resolves from the document root; the final index may name the one-past-end insert slot (append-shaped ops)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A·N     | `commit-paths.ts` `checkCommitPathAddressable` · commit primitive (pre-mutate, `assertCommitPaths`) · `commit-paths.test.ts`                                                                                                                                                                                                                                                                                                                                                                            |
| G1.17 | No late opener: an opener registered after the grammar was consumed warns — parsed documents never re-parse, so the kind silently misses every open document                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A·N     | `registry.ts` `checkLateOpenerRegistration` · bootstrap · `test/schema/registration-checks.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                     |
| G1.18 | ReservedChrome coherence: a `reservedChrome` declarer is a container, and its chrome kind resolves to a descriptor + component (both registered by `registerChromeLeaf`) — bootstrap counterpart to G1.14 per-commit slot check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | A·N     | `registry.ts` `checkReservedChromeCoherence` · bootstrap · `test/invariants/reserved-chrome-coherence.test.ts`, `test/schema/registration-checks.test.ts`                                                                                                                                                                                                                                                                                                                                               |
| G1.24 | Closure-block coherence: a kind's required `closure` block agrees with the rest of its descriptor — a container (declares `container.contract`) cannot claim `roundTrip: inherit-default` (its `rebuildRaw` IS the mechanism); a `not-mergeable` kind cannot claim `mergeBackspace: inherit-default` (no default merge to inherit); a cell claiming the focus-then-delete model must be backed by `blockFocus: 'whole-block'`, the declaration that routes the caret-adjacent merge fallbacks to a focus move; and a `reservedChrome` declarer cannot leave `clipboard: inherit-default`, its chrome bytes living in the container's own opener line. A declared `conformanceFixture` parses to a tree containing its kind — the fixture rule runs in the unit sweep, not the flush, because a `parse` import there closes a `schema → core/parser → schema` cycle                                                                                                                                                                                                                                                                                                                                                | A·N     | `registry.ts` `checkClosureCoherence` · bootstrap (registration rules) + unit sweep (fixture rule) · `test/invariants/closure-coherence.test.ts`, `test/invariants/closure-fixtures.test.ts`                                                                                                                                                                                                                                                                                                            |
| G1.25 | Widget-pool pass bracket: every `acquire` sits inside an open `beginPass`/`sweep` bracket, `beginPass` never opens over an unswept pass, `sweep` never closes an unopened one — outside a bracket, adoption flags and pass tallies are meaningless and byte-identical duplicate widgets are indistinguishable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | A·N     | `inline-transitions.ts` `checkPoolBracket` · own seam (`components/blocks/widget-portal.ts`) · `inline-transitions.test.ts`, `test/blocks/widget-portal-bracket.test.ts`                                                                                                                                                                                                                                                                                                                                |
| G1.26 | Reveal transition legality — the fold and the reveal must each imply the other, so the ID carries both directions. **No fold without a reveal:** a fold entry (`commitReveal`/`cancelReveal`/`foldRevealNoEdit`) runs only with an active reveal (every caller pre-guards — a bare fold is a caller that skipped the guard). **No mutation with a reveal** (`command-during-reveal`): no arm of a block's command dispatch mutates while a reveal is open, because the reveal holds the block's live bytes in ephemeral DOM the CST has not seen and every arm reads `node.raw` — a fire is a command entry path that skipped the fold. Also: `startReveal` is never re-entered inside the settle window (it spans only microtasks, so no user gesture can land there); the kernel's source-length precondition holds at reveal entry. Legal bails stay silent by design: blur with no reveal, the cross-block keep-revealed bail, entry while a reveal is already active                                                                                                                                                                                                                                         | A·N     | inline closures at `components/blocks/text/widget-interaction.ts` (fold legs) and `components/blocks/text/TextEditableBlock.svelte` `performBlockCommand` (the mutation leg) + `inline-transitions.ts` `checkRevealSourceLength` at the kernel (`cursor/reveal-source.ts`) · `test/blocks/text/widget-reveal-transitions.test.ts`                                                                                                                                                                       |
| G1.27 | Composition window: `compositionend` lands only inside a composition the surface saw start — browsers pair the events per element, so an unpaired end means a consumer wired `compositionend` without `compositionstart` and every composed keystroke committed to the CST mid-IME                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A·N     | `inline-transitions.ts` `checkCompositionEndPaired` · own seam (`components/blocks/editable-surface.ts` `onCompositionEnd`) · `inline-transitions.test.ts`, `test/blocks/editable-surface-composition.test.ts`                                                                                                                                                                                                                                                                                          |
| G1.28 | Rendered text fidelity: a code block's rendered fragment carries the block's bytes exactly (`textContent === trimTrailingLineEnding(raw)`) — the body round-trips through `template.innerHTML`, so the HTML parser's normalizations (U+0000 dropped outright, engine-defined line-ending and surrogate handling) could silently eat a byte the CST still holds, and the block reads `textContent` back on every keystroke commit (the indent, dedent and cut gestures read `node.raw` instead)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | A·N     | `render-fidelity.ts` `checkRenderedTextFidelity` · own seam (`components/blocks/code/code-renderer.ts` `renderCodeBlock`) · `test/invariants/render-fidelity.test.ts`                                                                                                                                                                                                                                                                                                                                   |
| G1.29 | Cross-block endpoint coordinates: an endpoint's offset means what its own block's coordinate space says. A table endpoint carries `cellCoordinate: true`, so it reads as a cell index and never as a character count — a table path IS cell space, and a char offset stored there routes rangeDelete down the generic branch and corrupts the grid. Both directions fire: a cell index stored against a block that has no cells is the same corruption from the opposite producer. Every other char endpoint lands inside its block's raw, and inside a kind with no character positions (childless `blockFocus: 'whole-block'`) on one of the two ends — an interior offset there slices an opaque unit in half, so copy truncates the syntax and delete destroys it. Same-path pairs are exempt (an intra-table rectangle's focus is unflagged by convention). Each rule has a normalizer meant to make it unfireable, and each has been missed once (a length-1 table path, a character hit-test over a rendered diagram); the producers are fixed and this is the belt for producer N+1                                                                                                                       | A·N     | `selection-endpoints.ts` `checkCrossBlockEndpointCoordinates` · own seam (`selection/selection-state.svelte.ts` `enterCrossBlock`/`extendFocus`) · `test/invariants/selection-endpoint-coordinates.test.ts`                                                                                                                                                                                                                                                                                             |
| G1.30 | Merge-role vocabulary: every registered kind declares a `mergeRole` from the known set — an unknown role makes the merge dispatcher fall through silently on every gesture reaching the kind. A per-KIND fact, so it is checked once per registration rather than once per committed node (where it re-ran a constant per commit and still missed a bad registration until the first edit)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A·N     | `registry.ts` `checkMergeRoleVocabulary` · bootstrap · `test/invariants/category-fields.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                        |
| G1.31 | Inline-construct policy coherence: a policy row names a kind the inline vocabulary holds (a mistyped one is silent — the construct keeps the absent-row defaults), the marker-rewriting behaviors (`close-and-reopen` split, empty auto-unwrap) belong only to a revealable kind, whose markers a caret can bring back on screen, and no two mark rows claim one nesting rank or one command — a tie would leave which mark wraps the other, or which one a press toggles, to registration order. A plugin row's mark may not claim a built-in command id: the id already answers somewhere, and the surfaces rank the mark lookup differently, so a `link.openCard` claim shadows the card on cells, not on prose. That arm guards a door that is not open yet: the policy registrar is on no public barrel at 1.0, so a row on a plugin-declared inline kind is reachable only from inside the repo (`docs/design/plugin-contract.md` § Inline authoring). Checked at the EDITOR-MOUNT flush alone: the rows register with the descriptors, but the policy's function hooks patch in from the component layer, which a parse-only unit test — where the parser drains the same registration queue — never loads | A·N     | `registry.ts` `checkInlineConstructPolicy` · mount (`invariants/install.ts` `runStartupInvariantChecks`), with the built-in-command rule refused outright at `schema/inline-construct-policy.ts` registration and this check its belt · `test/invariants/inline-construct-policy-coherence.test.ts`, `test/schema/inline-construct-policy.test.ts`                                                                                                                                                      |
| G1.32 | Content-start Backspace coherence: a kind declaring `contentStartBackspace` also declares `getContentRange` — without the hook its content starts at raw 0, where the demote arm never fires and the declaration is silently inert, observable only as a keystroke that merged when it should have demoted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A·N     | `registry.ts` `checkContentStartBackspace` · bootstrap · `test/invariants/content-start-backspace.test.ts`                                                                                                                                                                                                                                                                                                                                                                                              |
| G1.33 | Landable caret: a block a caret is seated into, under an editable marker-hiding rung, paints at least one landable position. A surface whose every byte is a hidden marker run takes the next keystroke at an element boundary between `display:none` spans, and the engine picks the side — so a typed `#` came back as `a#`. Built-ins hold by construction (chrome standing over no content paints, live-mode.md § 4.1); the guard is for a plugin surface minting its own marker chrome. Fired from the editor root's focus seam, never from a door's own body: a door seats a caret by focusing its surface, so a caret door a consumer's own component owns inherits the guard instead of routing around it. The seam is per focus arrival, so a re-seat into a surface that already holds focus fires nothing. Reading is out of scope: it takes no keystrokes                                                                                                                                                                                                                                                                                                                                             | A·N     | `landable-caret.ts` `checkLandableCaret` · own seam (`components/Editor.svelte`, the editor root's `focusin` funnel) · `test/invariants/landable-caret.test.ts`, `test/invariants/landable-caret-doors.test.ts`                                                                                                                                                                                                                                                                                         |
| G1.34 | Split landing: the index a split's caret lands on is the one `splitNode` returned, never a re-derived `blockIndex + 1`. The top-level path seats the caret at it and the list-item path splices at it, so both cross the seam on their way to using it. The constant is the second half only while the first half stays ONE block; bytes that reparse plural (a blank line inside indented code) push the second half down, and the constant seated the caret on the first half's tail (GH #98). The `tree-ops` warn beside it reports that the plural shape occurred, which is legal; this reports that a landing disagreed, which is not                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A·N     | `split-landing.ts` `checkSplitLanding` · own seam (`tree-operations/node-ops.ts` `assertSplitLanding`, crossed by `editor-actions/block-edit-core.ts` and `editor-actions/list-context.ts`) · `test/invariants/split-landing.test.ts`                                                                                                                                                                                                                                                                   |
| G1.35 | Single-node sink: a sink that installs exactly one node never installs bytes that reparse to several. Both merge doors reach one — the forward one reparses the concatenation, the deep-leaf one writes prev's leaf — and a join whose bytes read as two blocks is refused there rather than truncated to the first (a line vanishes from the document) or written whole into the surviving slot (the tree stops agreeing with its own reload). Both doors then return noop and the caret moves across the boundary instead (GH #166). The question is asked at the WRITE, over the nodes going into the slot, so the guard answers for sink N+1: one that skips the refusal its siblings make, or splices a plural replacement where one node belongs. Arriving plural is legal; installing plural is the fire                                                                                                                                                                                                                                                                                                                                                                                                   | A·N     | `single-node-sink.ts` `checkSingleNodeSink` · own seam (`tree-operations/node-ops.ts` `reparseAsNode`, `mergedLeafFor`) · `test/tree-operations/merge-multi-block-refusal.test.ts`                                                                                                                                                                                                                                                                                                                      |
| G1.36 | Structural-descriptor coherence: a `StructuralChange` fits the array it syncs — no negative slot count, no window past the end — and every publish seam leaves one id per child. `Array.from({length: -1})` is `[]`, so a descriptor derived from a LENGTH DIFF over a fixed window desyncs ids from children in silence the moment a splice moves slots the window never named: a settle folding a seam ABOVE a range delete is exactly that, and it shipped unobserved because nothing compared the published id array against the children. The producer half fires at the applicator, which every descriptor crosses; the consumer half fires at the two commit publish seams, because a descriptor can fit its own array while describing the wrong window. `childSpans` is the same shape of parallel array and gets the same reading, one span PAIR per child, over the nodes a commit touched: a rebuild seeding the wrong length, or a shape change that outlived its drop, is a stale-region splice waiting for the next keystroke                                                                                                                                                                      | A·N     | `structural-descriptor.ts` `checkStructuralDescriptor`, `checkIdsChildrenLockstep`, `checkChildSpansLockstep` · own seam (`tree-operations/structural-change.ts` `applyStructuralChangeToIdsRefs`) + commit primitive (`editor-actions/commit/undo-controller.ts`) + the per-commit node check (`invariants/install.ts` `assertCommittedNodes`) · `test/invariants/structural-descriptor.test.ts`, `test/schema/child-spans.test.ts`, `test/selection/cross-block/cross-block-delete-seam-fold.test.ts` |
| G1.37 | Descriptor-field coherence: field pairs the registration shape can represent and no kind can mean together, checked against the declarations alone. `contextDependentKind` beside a registered opener suppresses the reparse for a kind the parser CAN recognize, so its bytes stop re-deriving; `blockFocus: 'whole-block'` beside `supportsInline` parses inline constructs into a surface whose only addressable offsets are 0 and its display length; `blockFocus` beside `reservedChrome` declares the focus-then-delete model on a kind the chrome slot keeps from ever being childless. Each is silently inert rather than loud, so nothing fails until a gesture reaches the kind. G1.24 is the sibling over the closure cells; this one never reads them                                                                                                                                                                                                                                                                                                                                                                                                                                                 | A·N     | `registry.ts` `checkDescriptorFieldCoherence` · bootstrap · `test/invariants/descriptor-field-coherence.test.ts`                                                                                                                                                                                                                                                                                                                                                                                        |

### Ceremony guards (G1.19–G1.23)

These five fire on the same `[aragonite:invariant:…]` channel as the rest, and are enforced identically at runtime — but they are **inline closures at their own seam**, not shared predicates, for the reason given in § How a guard is built: their subject is a value that exists only during the commit or parse that builds it.

Their regression net is therefore not a unit test but the **e2e invariant watcher**: the shared `test` fixture (`src/lib/e2e/fixtures.ts`) fails any spec whose page emits an `[aragonite:invariant:…]` console fire, and the note-taking simulation drives the ceremonies hard enough to trip them. A spec whose subject is the fire declares it by tag (`test.use({ expectInvariants: ['the-tag'] })`), and the declaration is bidirectional: the named fire must arrive. `column-scope-alignment` is the reason that watcher exists: it was observable by no gate at all.

| ID    | Invariant                                                                                                                                                                                                          | Codes | Seam                                                                                                        |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- | ----------------------------------------------------------------------------------------------------------- |
| G1.19 | Multi-scope commit path: a scope's declared path still resolves to its captured node, checked over all scopes _before_ any spine is unshared (a stale-but-in-range path would unshare and rebuild the wrong spine) | A     | `editor-actions/commit/undo-controller.ts`                                                                  |
| G1.20 | Unshared-spine depth: the chain `withUnsharedSpine` hands back is as deep as the leaf path it was given — a short chain means the caller mutates a node it does not own                                            | A     | `editor-actions/nested/nested-block-edit.ts`                                                                |
| G1.21 | Column-scope alignment: each row scope in a column edit _is_ the corresponding child of the owned table, so the id/ref sync lands on the rows the splice actually walked                                           | A     | `editor-actions/table-context.ts`                                                                           |
| G1.22 | Unshare path in range: every index on an unshare path addresses a live child — an out-of-range index silently truncates the chain                                                                                  | A     | `tree-operations/unshare.ts` `ensureUnsharedPath`                                                           |
| G1.23 | Decoration sources never run inside the commit ceremony: `notifyEdit`/`runAll` assert the ceremony's in-progress flag is clear — a source running mid-commit would read a half-published tree                      | A     | `decorations/decoration-state.svelte.ts`, over the `invariants/commit-scope.ts` flag the commit helper sets |

### Notes on the tricky ones

**G1.1 is byte-level, not structural.** It compares `strip(raw)` (the re-parsed correspondent's stripped-inner bytes) against `serialize(children)` — exactly the documented invariant. It deliberately does **not** structurally compare the re-parsed tree to `node.children`: an empty editable container holds a single empty-paragraph placeholder so it has a focusable leaf, which the parser emits as `innerSuffix`/trivia for byte-identical output (`- \n`, a trailing blank `>` line). Both forms satisfy the invariant. Don't re-tighten to a structural tree compare — it false-fires on every empty list item and trailing blank quoted line, and that false positive went unseen until the simulation oracle was wired to the invariant channel. It does require the reparse to be a SINGLE block (for `listItem`, a wrapping list holding it alone), which is not that compare: bytes belonging to a following sibling leave the first block's inner content intact, so without it a container whose raw has grown reads as faithful.

**G1.2 exempts `listItem` from the component check.** It has no component-registry entry by design — items render inside their parent `ListBlock`, never via a `BlockHost` kind lookup — so the predicate skips the component check for it (`NO_STANDALONE_COMPONENT`), keeping the bootstrap channel free of a benign per-mount warning; the `BlockHost` visible-raw fallback still covers any stray lookup. `tableRow`/`tableCell` are registered as raw-block fallbacks, so `listItem` is the sole exemption.

**G1.9's copy-path-on-write follows the `$state` canonical-reference discipline** — after splicing a copy into the live tree, re-read it through the tree before further use. `tree-operations/unshare.ts`'s header owns the full statement.

**Commit-rollback contract.** Where G1.9 guards against a mutation corrupting a _shared_ snapshot, this guards against a _dangling_ one: a commit mutation that throws must leave the undo/redo stacks byte-identical to their pre-commit state and must not publish a partial tree. `__commit` captures both stacks before the snapshot push and, on throw, restores them via `UndoManager.restoreStacks` (a wholesale restore that also recovers an entry the push evicted at `MAX_UNDO`), emits `error{origin:'commit'}` on the event seam, then re-throws in DEV / swallows in production.

The two commit branches keep the tree intact differently. The **document branch** mutates a detached children copy and publishes it only on success, so a throw leaves the live tree untouched. The **container/multi-scope branch** mutates the live tree in place (its scope views are windows onto live nodes), so `__commit` captures the top-level children array before the mutation and swaps it back on throw — copy-path-on-write guarantees the pre-mutation array still reaches an intact tree at every depth, discarding every copy the mutation dirtied.

The array swap alone cannot reach a `snapshot:'skip'` container commit whose scope node was already unshared earlier in the same undo unit: copy-path-on-write is then a no-op, so the mutation's structural splice lands in place on a node the pre-mutation top-level array still references — reachable through cross-block paste, where one snapshot push joins a delete and a paste that both commit as `snapshot:'skip'` structural splices. `__commit` closes this by also capturing each prepared scope's pre-mutate children/childIds arrays and reinstating them on throw (doc-scope skip commits were already recovered — there the mutated array is `deps.doc.children` itself).

One residual is open by design. The frame's byte registers reach each prepared scope's spine and its direct children (`savedRaws`) and the document's folded trailing line (`savedDocSuffix`), so what stays uncovered is narrower: a write deeper than an owned node's direct children, node metadata, and `leadingTrivia` on a node the document branch already owns. For a scope already unshared earlier in the same undo unit, copy-path-on-write is a no-op, so such a write leaves those bytes changed after a throw rolls the structure back — structure and bytes then disagree. It is out of scope for pre-publish corruption (the user mutation and the arity check both throw before any byte write) and only reachable via an internal malformed-change bug, which DEV re-throws. Covered by `test/editor-actions/commit/commit-rollback.test.ts` and `commit-rollback-bytes.test.ts`.

## Group 2 — Property/regression-tested (P·N)

Test files under `test/invariants/` (arbitraries in `test/invariants/arbitraries/`).

| ID    | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Codes | Test                                                                                                                               |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------- |
| G2.1  | Round-trip + parser totality over arbitrary strings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | P·N   | `round-trip.property.test.ts`                                                                                                      |
| G2.2  | EOF edge states round-trip (unclosed fence, unterminated HTML)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | P·N   | `round-trip.property.test.ts` (G2.2 block)                                                                                         |
| G2.3  | Inline conformance corpus                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P     | `inline-conformance.test.ts`                                                                                                       |
| G2.4  | `textContent === ambientPrefix + raw` spine (jsdom)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | P     | `textcontent-spine.property.test.ts`                                                                                               |
| G2.5  | Inline-tree offset partition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | P·N   | `inline-offsets.property.test.ts`                                                                                                  |
| G2.6  | Serialization purity (ignores metadata/editor-level fields)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P     | `serialization-purity.property.test.ts`                                                                                            |
| G2.7  | Selection partition + `walkBetween` order                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P     | `selection-partition.property.test.ts`                                                                                             |
| G2.8  | Split/merge round-trip + id↔ref↔children alignment (all scopes)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | P·N   | `structural-id-ref-alignment.test.ts`                                                                                              |
| G2.9  | Paste op-kind emission, chosen by strategy, not by target depth: default structural → `replaceBlock`, container absorb/merge → `paste`, cross-block inline → `updateContent`. A consumer counting pastes watches all three                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P     | `paste-op-kind.test.ts`                                                                                                            |
| G2.10 | Sticky-column matrix + capture-without-reset and keydown-door guards (jsdom)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | P·A   | `sticky-column-matrix.test.ts`, `lint/sticky-column-capture-reset.test.ts`                                                         |
| G2.11 | Inline scan total coverage + construct tiling + kind vocabulary (built-in kinds plus those an installed plugin declared, so the property also runs with the bundled rungs registered)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | P     | `inline-total-coverage.property.test.ts`                                                                                           |
| G2.12 | A caret placement ends a live cross-block range, unless it is an extend. The programmatic door is a funnel — `BlockComponent.focus` is minted over each surface's park primitive and ends the range itself — so the lint carries the three parts that cannot be funnelled: NATIVE caret placement (a click's own default moves the caret, so per-file pointer-door declarations still apply), the park verb's caller allowlist (legitimacy is the caller's intent, and no position test separates the uses), and the park verb's presence on every LEAF that forwards a shared caret seam (it is optional on the contract, so a missing forward type-checks). Containers left that class: each publishes its whole surface as one `containerApi` instance export, so the lint asserts that publication instead of a per-member pairing. Behavioral complement: the simulation's range-interrupt family drives the same precondition through real gestures | L     | `caret-gesture-range-reset.test.ts`, plus `src/lib/e2e/tests/simulation/range-interrupt-ops.spec.ts` (outside this section's root) |
| G2.13 | Shape fixed point: an edit on a loaded document leaves a tree whose serialization reparses to the SAME block shape. The complement of G2.1, which a shape loss passes untouched — the bytes were exact and one block was gone. Two lanes: blank-line-separated documents under the split/delete/commit gestures, the surface the blank-line rule governs, and inline-source paragraphs under the live split, the differential that judges the rebalancer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P·N   | `shape-fixed-point.property.test.ts`, `parse-convergence.test.ts`                                                                  |
| G2.14 | Toggle-ladder ⟺ active-read equivalence over a RANGE: where `isInlineFormatActive` is true the toggle unapplies, where it is false the toggle applies. The cross-block direction rests on it — one side growing a fourth arm misroutes writes in silence — and it holds with one arm outside it, in one mode: where the delimiters PAINT, the bare wrap writes its literal bytes unverified, on screen for the reader to see and fix (live-mode.md § 4.3). Split, absorb, the flank strip and the marker-hiding wrap all coverage-verify, the split over every covering run of the kind rather than the innermost alone; the aligned strip verifies coverage in no mode, firing only where the block's own parse holds the construct at exactly the selection and no second run of that kind covers it, with a screen check where the delimiters do not paint. A collapsed caret is a different ladder                                                    | N     | `format-toggle-ladder.test.ts`                                                                                                     |

## Group 3 — Compile-time type guards (T)

The top rung: the violation doesn't compile. Enforced by `npm run check`; no runtime seam.

| ID   | Guard                                                                                                                                                                                                                                                                                                         | Retires                                                                                                                                                                    | Status |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| G3.1 | `BlockMetadataByKind` + `metadataOf<K>`                                                                                                                                                                                                                                                                       | `as` metadata casts                                                                                                                                                        | landed |
| G3.2 | `defineBlockComponent`, whose exports parameter is the two sanctioned publication shapes (`BlockComponentExports`): a leaf's own surface, or a container's single `containerApi`                                                                                                                              | `as unknown as` casts; a container that publishes no surface at all                                                                                                        | landed |
| G3.3 | `SelectionPoint` is a discriminated union (`CharSelectionPoint \| CellSelectionPoint`) on `cellCoordinate`: a cell point needs the literal `true`, a char-typed slot rejects a cell point                                                                                                                     | the optional-`boolean` flag's construction/assignment ambiguity; `offset` keeps its name, so a wrong-space _read_ narrows to the `charOffsetOf`/`cellIndexOf` runtime belt | landed |
| G3.4 | Branded `CURSOR_END` / `SELECTION_END`                                                                                                                                                                                                                                                                        | `999999` magic number                                                                                                                                                      | landed |
| G3.5 | `containerContract: 'strip' \| 'grid' \| 'opaque'`                                                                                                                                                                                                                                                            | implicit table exemption                                                                                                                                                   | landed |
| G3.6 | `BlockKindRegistration` `container` group — container-only fields register as one unit with `contract`/`rebuildRaw` required, `isContainer` derived; leaf augments carrying a `container` group throw                                                                                                         | G1.3 runtime pairing guard                                                                                                                                                 | landed |
| G3.7 | Branded coordinate spaces (`cursor/coordinate-spaces.ts`): `RawOffset`/`DomTextOffset`/`EditorX`/`ViewportX`/`CellIndex`/`DocPath`, minted only at their home modules with named conversions per direction; public doors keep `number` and brand once at the boundary. Cross-space arithmetic is a type error | offset-space mixing (the audit's second bug class); `charOffsetOf`'s read-space check narrows to the runtime belt                                                          | landed |
| G3.8 | Bytes-readonly node views (`core/node-views.ts` `NodeView`/`DocumentView`): serialized bytes deep-readonly, `childIds`/`childSpans`/`ownerEpoch` bookkeeping writable — G1.9 stated as a type; readers hold views, constructors/writers keep `CstNode`, the unshare seam is the only door (G4.13)             | reader-side byte writes; the "read-only by contract" prose on `BlockComponentProps.document`                                                                               | landed |

## Group 4 — Lint/structural + harness

Source-scan tests, in TWO homes, and the split decides which script runs a scan. They catch what a type can't express and a runtime seam can't see: a pattern anywhere in the tree, or a published table drifting from the code that backs it.

Scans over the LIBRARY source live in `test/invariants/lint/` and ride `npm run test:editor:invariants`. Scans over the E2E TREE live in `e2e/lint/` (G4.22, G4.23, G4.49) and do **not** — that script is path-scoped, so they ride `npm test` through vitest's second include glob and are iterated with `npx vitest run src/lib/e2e/lint/`.

The table below is the catalogued set, not the whole of `test/invariants/lint/`: a scan guarding one seam's own local rule earns a file without earning a G-number, so read the directory as well as this table before assuming a rule is unguarded.

| ID    | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Codes   | Test                                                                                                                                                   |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| G4.1  | No by-value `createBlockListState` (getters only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | L       | `lint/createblockliststate-getters.test.ts`                                                                                                            |
| G4.2  | Render path computes inline via `computeInlineContent`, never the caching `getInlineContent` accessor (perf-hygiene: the cache is a non-reactive WeakMap)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | L       | `lint/render-inlinecontent.test.ts`                                                                                                                    |
| G4.3  | Container conformance kit + declaration sanity (unwrapRole strategies resolve, containerPaste shape, rebuildRaw runs). Built-ins are swept registry-derived; a plugin container opts in with its own profile through `runContainerConformance` (`@voithos-labs/aragonite/testing`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | harness | `container-conformance.test.ts` (built-ins), `plugins/container-conformance.test.ts` (plugin containers); kit: `$lib/testing/container-conformance.ts` |
| G4.4  | No timing hacks for sequencing (timing primitives allowlisted)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | L       | `lint/timing-hacks.test.ts`                                                                                                                            |
| G4.5  | No synthetic `KeyboardEvent` in editor runtime source (cross-block redispatch hack retired)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | L       | `lint/no-synthetic-keyboard-event.test.ts`                                                                                                             |
| G4.6  | CSS ownership: `app.css` holds no editor rules/tokens; every editor-owned token read is declared in `editor-theme.css`; every host-token read carries a fallback; host-chrome defaults sit behind the opt-in theme class alone (G4.6d), so a themed host's cascade reaches the editor unbridged. The same rule extends to the dogfood/reference plugins, which may also read tokens they declare themselves                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | L       | `lint/css-ownership.test.ts` (editor), `lint/plugin-css-ownership.test.ts` (dogfood plugins)                                                           |
| G4.7  | Render-memo completeness: a block's render memo key includes every input its built DOM embeds; no live value (tree path, index, load policy, checkbox state) is baked as a build-time snapshot the key omits — resolve it live or key on it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | D·N     | `widget-path-restability.spec.ts` (path); `render-image-policy.test.ts`, `task-checkbox.test.ts` (policy/checkbox); G4.2 enforced sibling              |
| G4.8  | Documented-chord dispatch: every chord the consumer guide's keyboard table lists resolves in the surface that actually dispatches it — the keymap registry (which the table's structural chords joined at 0.9.36), the search components, or the clipboard seams (the whole-block key tail and the text block's clipboard seam)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | L       | `lint/consumer-guide-chords.test.ts`                                                                                                                   |
| G4.9  | Theme-token manifest: every token the consumer and plugin guides publish is declared in `editor-theme.css`, and a themed token carries both a light and a dark value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | L       | `lint/theme-token-manifest.test.ts`                                                                                                                    |
| G4.10 | Plugin package/pack parity: every `src/lib/plugins/<name>` dir is exported as `./plugins/<name>`, from which verify-pack derives its REQUIRED manifest (`index.js` + `index.d.ts` via `scripts/pack-manifest.mjs`); a plugin module with a top-level CSS import is declared in `sideEffects`. Subset, not equality — `/renderer` engine subpaths are extra                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | L       | `lint/plugin-pack-parity.test.ts`                                                                                                                      |
| G4.11 | Paste-transform two-site parity: exactly the two sanctioned clipboard→parse routes call `applyPasteTransforms`; a third route born without it silently drops plugin transforms. Two arms, because caller parity alone cannot see the shape it names — a route that never mentions the symbol contributes nothing to the caller set — so every clipboard or drop read is also enumerated and required to reach a sanctioned route                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | L       | `lint/paste-transform-sites.test.ts`                                                                                                                   |
| G4.12 | Caret-edge destructive-key funnel: every plain Backspace/Delete intercepted at a caret edge in a prose block routes through the one edge-policy dispatch (which resolves CST widget / decoration island / ambient overlap against declarative policies) and commits via `updateBlockContent`; the only carve-out is the selected-widget second-press delete; no other `blocks/text/` file intercepts a plain destructive key unallowlisted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | L       | `lint/caret-edge-seams.test.ts`                                                                                                                        |
| G4.13 | View→mutable door: no `as CstNode`/`as Document` view-stripping cast outside `tree-operations/` and the commit ceremony — readers hold bytes-readonly views (`core/node-views.ts`, G1.9 as a type) and re-enter mutability only through the unshare/clone seam or a commit scope's owned view                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | T·L     | `lint/view-stripping-casts.test.ts`; type pins in `test/core/node-views.test.ts`                                                                       |
| G4.14 | Readonly-view prop parity: every `.svelte` component prop reading the CST is typed `NodeView`/`DocumentView` — the registration boundary erases prop types, so a `node: CstNode` drift compiles; only the doc-owning root (`Editor.svelte`) holds a mutable `Document`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | L       | `lint/readonly-view-prop-annotations.test.ts`                                                                                                          |
| G4.15 | Coordinate-brand mint discipline: `as <Brand>` casts and `as*` boundary mints appear only in `cursor/coordinate-spaces.ts` and the allowlisted public-door files — everywhere else arrives at a brand through a mint or a named conversion (G3.7's runtime-source complement)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | L       | `lint/coordinate-brand-mints.test.ts`                                                                                                                  |
| G4.16 | Bundled-plugin import boundary: every file under `src/lib/plugins/**` imports only the public authoring barrel (`$lib/plugin`), its own plugin dir, `svelte`, or — for a `renderer.ts` — its one declared rendering engine; the dogfood proof that the authoring barrel is complete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | L       | `lint/plugin-import-boundary.test.ts`                                                                                                                  |
| G4.17 | Perf spec glob partition: every `*.spec.ts` under `e2e/tests/perf/` is collected by `e2e-vr` or by `e2e-perf`/`e2e-perf-prod` — a spec matching neither runs in no Playwright project and is silently never executed. Classified by path relative to the perf dir, not by basename, because the two projects differ in depth: `e2e-vr`'s `vr-*.spec.ts` cannot cross a `/`, while `e2e-perf`'s `*.perf.spec.ts` matches at any depth                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | L       | `lint/perf-glob-partition.test.ts`                                                                                                                     |
| G4.18 | Inline-trigger parity: `BUILTIN_TRIGGERS` (`core/inline/scan/plugin-syntax.ts`) equals the `scanInline` switch `case` labels (`core/inline/scan/index.ts`) — a trigger the switch claims but the set omits would be accepted by `registerInlineSyntax` and then silently shadowed. Also partitions every reserved trigger across the three routes that reach the scan — scan-visible (`SPECIAL_CHARS`), scan-probed (`SCAN_PROBED_RESERVED`), rejected (`REJECTED_RESERVED`) — so a trigger with no route, two routes, or a route it does not qualify for fails here; and pins the pre-switch prefix consultation to one site ahead of the switch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | L       | `lint/inline-trigger-parity.test.ts`                                                                                                                   |
| G4.19 | Reading-gate two-arm parity: every `dispatchKeyCommand`/`dispatchKindCommand`/`runCommandById`/`runGlobalChord`/`runGlobalChordOnKind`/`getCommand`-direct construction site either threads `getPresentationMode` (the seam dead-keys reading mode) or carries a local reading/readOnly guard in its handler (allowlisted with the guard token); the dispatcher definitions + the seam's post-gate `getCommand` are excluded. Threading resolves from the CALL SITE: the getter inline in the argument list, or inside the object literal a named context argument binds, so the `runCommand` door rewired to a context that skips the getter fails. A new editable surface skipping the gate trips the file-set check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | L       | `lint/reading-gate-parity.test.ts`                                                                                                                     |
| G4.20 | Trailing-line-ending reconstruction parity: a site that reattaches or mints a line ending reads it from the bytes it is standing in for (`trailingLineEnding`), never a bare newline literal (which downgrades a CRLF block to LF and breaks byte round-trip). Five source-scan arms — no `updateBlockContent` content argument reconstructs with a string-literal newline; every `commitInput` funnel reaching `updateBlockContent` appends the ending (table cells allowlisted: a GFM cell holds no raw newline); no container `rebuildRaw` emits a newline literal into the bytes it re-derives; the ending ternary and the complement reading what a block's own bytes carry both live in `core/lines.ts` alone, so neither inline idiom can seed the next copy; and no write to a node's `raw` mints a newline literal at all — the domain arm, which reaches the rebuilders, the list terminator and the range-delete branches that sit outside both funnels, with legitimately-literal writes allowlisted by reason AND count. The scans see literal shapes only, so an outcome-level oracle runs each gesture over an LF fixture and its CRLF mirror and requires the results to mirror — it catches the mint sites (defaulted parameters, placeholder paragraphs) no shape matches, and fires for gesture N+1 untaught                                                                                                                                                                                               | L·N     | `lint/trailing-line-ending-parity.test.ts` (arms); `crlf-edit-mirror.test.ts` (outcome oracle)                                                         |
| G4.21 | Image byte-write seam — a **name-presence file-set scan**, not a behavioral one: the GFM serializer is named in code only inside the seam module, and exactly the documented write paths name `buildImageEditBytes`; the popover is checked separately for naming neither, since it takes the seam's answer as a prop rather than by import. It catches the shape that shipped — three write sites (commit, keyboard resize, popover dirty check) each reaching for the serializer directly and each re-emitting GFM over bytes an inline rung claimed — and a fourth path joining the funnel undocumented. It does **not** catch a path that hand-rolls the GFM bytes from a template; no name scan can. Scanned rather than typed because the serializer carries its own unit suite and so cannot be unexported                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | L       | `lint/image-bytes-write-seam.test.ts`                                                                                                                  |
| G4.22 | Settle-predicate vacuity (e2e): inside one `test()` body, a `waitForSource*` predicate must describe the POST-operation shape — one already true on the document the test loaded returns on its first poll, so it synchronizes on nothing and a gesture that silently no-ops satisfies the whole chain                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | L       | `e2e/lint/settle-predicate-vacuity.test.ts`                                                                                                            |
| G4.23 | Requirement↔spec lockstep (e2e): every spec under `e2e/tests/` pairs with a requirement file under `e2e/requirements/` and vice versa (`.perf` stripped from the stem, no two specs claiming one file); each requirement carries a title, a section and a scenario, each spec a `test()`; and a requirement list 3× longer than its spec's test count is named with a reason in the scan's allowlist. Count EQUALITY is refuted by measurement (most pairs diverge legitimately) — the dated census is in the file header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | L       | `e2e/lint/requirement-spec-lockstep.test.ts`                                                                                                           |
| G4.24 | Code-surface commit funnel: `CodeBlock.svelte` holds exactly one `updateBlockContent` call and it is `commitDisplay`'s, so the fence write reconciliation (escalate a body line the parser would read as this block's closer, drop a backtick the info string cannot hold) runs on every display commit rather than at the gestures that remembered it. Two that predated the seam — Enter splitting a line around a mid-line run, Shift+Tab dedenting an indented one to column 0 — split the block by moving bytes, adding no character                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | L       | `lint/code-commit-funnel.test.ts`                                                                                                                      |
| G4.25 | No `import.meta` env read anywhere under `src/lib`: it is a Vite-only extension, so outside a Vite bundle the object is undefined and a module-scope read throws at import time — the library will not load at all under another bundler. Toolchain flags come from `esm-env`, whose export conditions every bundler resolves and whose `DEV` still folds away in a production build. The one scan covering the test tree too, because `svelte-package` inspects everything it copies and warns on the token wherever it sits (it only warns, so a test-tree read would rot the packaging claim unwatched). Library-scoped rather than repo-wide: the reference plugins and the consumer example are Vite APPS, where the read is legitimate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | L       | `lint/no-import-meta-env.test.ts`                                                                                                                      |
| G4.26 | Comment blocks stay inside the budget: a file's first block (the header) holds at most ~7 text lines, any other block at most 6. The stated budget (1-2 lines, headers ~5) was documented-only and drifted exactly as the enforcement ladder predicts; this gate catches the essay class and leaves the finer register to review. A why that needs more lines belongs in a design doc, a requirement file, or the commit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | L       | `lint/comment-budget.test.ts`                                                                                                                          |
| G4.27 | Parse-scope declaration: every call of the core `parse` entry outside `core/parser.ts` passes an explicit `scope`. The default is `'document'`, so a silent fragment caller (a commit reparse, a clipboard parse, a container body) hands one block's bytes to the openers as a whole document, and a position-scoped kind mints wherever the edited block sat (issue #52). Scope is the caller's knowledge alone — nothing in `parse` can recover it — so the declaration lives at the call. Scanned over the library and the plugin-route author stand-in; the consumer example is excluded (it writes the documented whole-document default), as are the published kits (fixtures are whole documents)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | L       | `lint/parse-scope-sites.test.ts`                                                                                                                       |
| G4.28 | Leaf raw-write rule parity: a kind's own `normalizeRawWrite` reaches its bytes through two readers in `node-ops` — `writeOwnRaw` for a sink that writes in place, `normalizeOwnRaw` for one that replaces the leaf with a reparse of the result — and exactly the documented sinks call each (find/replace's private clone, the same-block range merge, the degraded typed-char splice, the container-matching paste; the cross-block merge and the truncated-endpoint reparse), plus the table branch, which inherits the rule by routing through that shared reparse rather than naming a reader. G4.24 pins the code SURFACE's write sites, which is why find-and-replace could write a fence terminator into a code body (issue #45) and a delete past a closer could drop one (issue #55): the descriptor-hook route was unwatched, and a reparse re-derives honest metadata that no longer knows what the truncation took. The companion arms pin the fence rule to one implementation, in `schema/` rather than under `components/` so a headless sink reaches it                                                                                                                                                                                                                                                                                                                                                                                                                                                      | L       | `lint/leaf-raw-write-rule.test.ts`                                                                                                                     |
| G4.29 | Hardcoded-chord manifest: every library file that reads a KeyboardEvent modifier flag is named in `schema/reserved-chords.ts`, with the chords it claims outside the keymaps and the key literals it compares. A new claiming site, or a new key compared in an existing one, fails the gate until the entry is re-derived — which is what keeps the public `reservedChords()` from rotting. Authoring constraint: a manifested file must keep literal key comparisons and literal modifier reads, since the scan is structural on both axes — factoring either behind a shared helper fails the gate until the scan learns that helper                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | L       | `lint/reserved-chord-manifest.test.ts`                                                                                                                 |
| G4.30 | Hidden-run classification one rule, two spaces: `core/inline/visibility.ts` states the marker families and the hiding rule, and `cursor/widget-offset.ts` applies it where there is a caret — the walk's landing rule, the read canonicalization, the widget-free walk beside it, and the block-edge gates that ask whether a block's own markers paint all read that one answer, and a third copy disagrees the day a mode or a reveal rule moves (a caret seated in unpainted text corrupts silently, since the next read-back is browser-normalization-dependent). The node-space and DOM-space answers are held together by a property test over one rendered fragment, not by convention. Two census arms: resolving hiding state either way (the DOM-read shapes — mode root, block-focus stamp, construct stamp, marker-class selectors — or a call to the model's own rule) is allowlisted to the two homes plus the readers asking a different question (ambient span identity, the preview-inline reveal writer), and every file naming a marker class in code is manifested with its role, so a new namer is a decision rather than a drift. The runtime belt is the DEV parity probe (`invariants/marker-css-parity.ts`): once per mode change, a probe span per family compares `getComputedStyle` against the predicate's answer, standing down where no stylesheet computes — the presentation e2e battery asserts it through the invariant-console gate                                                       | L       | `lint/hidden-run-classification.test.ts`                                                                                                               |
| G4.31 | Affinity reaches every sticky seam, and the pending marks reach every affinity seam: all three are ephemeral caret state with one lifetime — an arrival sets them, a commit invalidates them — so every file that calls `stickyColumn.reset()` clears the affinity at least as often, and every `noteKey` capture door also calls one of the affinity's classifiers. There are THREE doors, and the scan names all three: `note(e)` classifies a keydown, `noteTyping()` pins the committed byte, and `noteExtreme()` says the caret was SEATED at an extreme rather than stepped there (a range collapsing onto its own edge) — the last one counts on both axes, since settling a side also invalidates the marks riding it. The column paid for that parity at N−1 of N sites (G2.10); the affinity inherits the rule as a scan instead of at the next audit, because a side that survives an edit is spent by the typing seat on the wrong byte. The marks take it one rung higher — one construction composes them onto the affinity's invalidation, so the scan pins that composition, forbids a clear at any seam of their own, and holds the set-equality list of seats that spend them. Exception maps are empty by design — an entry is a stated hole                                                                                                                                                                                                                                                               | L       | `lint/edge-affinity-capture-reset.test.ts`                                                                                                             |
| G4.32 | Inline-cache one spelling: every non-render consumer reads the inline tree through `resolvedInlineContent`, so the resolver AND the signature travel together. The accessor keys one sub-entry per signature space, so a caller that passes the resolver and drops the signature reads the entry the render path did not fill and answers with brackets where the screen shows a link — the class that produced the bounds seam. The raw `getInlineContent` stays inside its own module; the one allowlisted caller is the vertical-skip decision, whose resolver-less answer is its stated contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | L       | `lint/inline-cache-one-spelling.test.ts`                                                                                                               |
| G4.33 | Live-rewrite verification: the modules that build live-mode byte candidates each verify through the render path's own `renderedText`, every file naming an inline marker family in code is manifested with what it does with it (only the oracle decides which spans a marker-hiding mode DROPS; the rest mint, identify or probe), and the two registered seam slots (split rebalancer, join cleaner) have exactly ONE reader — `node-ops`, so every destructive join crosses `cleanJoinedRaw` rather than writing its own concatenation. The class is a private walk over the parse disagreeing with what paints: it counted an angle autolink's brackets as content once, and a resolved reference's label the next time. Each verification also states WHICH reading it takes: the block's own screen where the answer decides what a press may touch, the content behind every marker family where it is a before/after conservation diff, since chrome folds the moment content arrives. The content reading carries a precondition, not a blanket license — a seam taking it declines any side whose chrome PAINTS (live-mode.md § 4.1), or it reads bytes on screen as bytes nobody saw and drops them. The two cut seams run off the DOM and answer that from the model, since a construct wrapping content-empty chrome paints its own delimiters too. The files permitted to name `preDelete` at all are a closed allowlist, each with its reason — the fenced-code surface being the one splicer outside the seam | L       | `lint/live-rewrite-verification.test.ts`                                                                                                               |
| G4.34 | Link byte-write seam — the image seam's twin (G4.21) over the construct whose destination the reader never sees: the GFM link serializer is named in code only inside `link-source-bytes.ts`, and exactly the documented write paths name `buildLinkEditBytes`/`buildLinkUnwrapBytes`. The serializer is module-private, so what the scan adds over the module boundary is the CALLER set — a second write path is the shape that shipped for images. The seam also verifies every candidate through the render path (G4.33): a destination that breaks its own construct surfaces as literal source, which no walk over the parse can see. A rung-claimed link is declined outright, there being no `rewriteLink` hook to re-serialize it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | L       | `lint/link-bytes-write-seam.test.ts`                                                                                                                   |
| G4.35 | Stamp↔revealable parity: a construct whose marker spans carry a `data-construct-*` stamp declares `revealable: true` in the inline-construct policy table, and every revealable kind stamps — the census is derived from `core/inline-render.ts` (which helper calls `tagConstruct`, which `renderNode` case reaches it), so a stamp without a policy row reveals nothing and a policy row without a stamp addresses nothing. For this scan an ABSENT row reads as `revealable: false` (`?.revealable === true`), which is a scan rule, not a ruling on absent-row semantics: `escape`, `hardLineBreak` and `autolink` all carry rows saying false (an autolink's `<`/`>` hide with the block rather than by reveal, and its row is what seats a typed byte outside them), so an absent row today means a kind with no declared live-mode behavior at all                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | L       | `lint/stamp-revealable-parity.test.ts`                                                                                                                 |
| G4.36 | Caret-write doors: no module outside the door homes writes a caret position — G4.33's twin over seats instead of bytes. A caret write is where a raw offset becomes a DOM seat, and every home applies the landable clamp or forwards to one that does (the park door and `applyCollapsedCaret` hold the clamp; the rest forward), so a caller cannot seat a caret behind a hidden marker run and hand the typing seat a position no arrow walk produces. Five name-level censuses with per-file reasons and set equality: `setRaw` namers (one per cursor backend), `setToAmbientBoundary` (one home), native `addRange`/`setBaseAndExtent` writers, the two caret-write helpers (`setCursorOffset`, `restoreCaretAtWalkOffset`), and the surfaces minting `focus` from `caret-doors`' `placeCaret`. A new writer in any census is a lint conversation, not a drift — the class arrived as door-by-door sentinel sweeps that did not converge until the count was closed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | L       | `lint/caret-write-doors.test.ts`                                                                                                                       |
| G4.37 | Walk-container↔content-empty-stamp parity: the files rendering a fragment into a contenteditable the caret walk reads (`renderInlineNodes` / `renderCodeBlock`) are exactly the files stamping `data-content-empty`. The three stamp sites are verbatim copies with no funnel, and G1.33 catches the shape only when a caret parks into it in dev, so surface N+1 shipped unstamped would paint a marker-only block as an empty line nobody can reach. Both censuses are pinned, and the files naming a renderer that mounts nothing carry a per-file reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | L       | `lint/content-empty-stamp-census.test.ts`                                                                                                              |
| G4.38 | Insertion-door surface parity: every component mounting an editable surface (`createEditableSurface` / `createEditableLeaf`) publishes `insertMarkdown` through the channel its own mount reads — an instance export, or the surface literal it hands `publishRefSlot` where nothing binds to the instance. The shared clipboard skeleton mints the door for all of them, but Svelte 5 instance exports have no spread and `BlockComponent` declares the method optional, so the last hop is hand-written per component: surface N+1 would compile clean and silently decline every `editor.insertMarkdown()` on its blocks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | L       | `lint/insert-door-surface-parity.test.ts`                                                                                                              |
| G4.39 | Command-door surface parity: every component mounting a command surface publishes `runCommand` as an instance export. G4.38’s twin over the semantic door, with a population wider by one signal: a component dispatching chords itself is a command surface even where no editable-surface factory minted it (the thematic break is `editable = false` and still takes commands). `BlockComponent` declares the method optional, so surface N+1 would compile clean and decline every `editor.runCommand()` on its blocks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | L       | `lint/command-door-surface-parity.test.ts`                                                                                                             |
| G4.40 | Rewrite-claim set parity: the ids the built-in keymaps bind to a rewrite over one block's own selection, the ids the dispatch seam answers specially over a cross-block range (`RANGE_DECLINED_COMMAND_IDS` declines, `CROSS_BLOCK_RANGE_COMMAND_IDS` routes to the arm), and the chords `selection/cross-block/keydown.ts` claims are one set, with each id on exactly one of the two lists. Membership is the arm's shape, not the id's prefix, so the census carries the non-`format.` members by hand — a prefix census could not see `link.openCard`. A sixth rewrite taught to one spelling is an N-1 gap at the other two; the swallow GROWING a chord is caught by G4.29 instead, whose manifest records the key literals that file compares                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | N       | `selection/cross-block/rewrite-claim-parity.test.ts`                                                                                                   |
| G4.41 | Warn-gate bypass census: no file under `src/lib` mocks `dev-warn` or spies `console.warn`. A mocked `devWarn` never reaches the structured sink, and a console spy reads a channel a registered sink silences, so the fail-on-warn unit gate goes blind for that whole file and every guard firing in it passes unnoticed. The one file whose subject IS a warning channel (`devWarn`’s console arm) is named in the scan’s allowlist with the reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | L       | `test/invariants/lint/warn-gate-bypass-census.test.ts`                                                                                                 |
| G4.42 | Separator-write-door census: no module writes a sibling's `leadingTrivia` by hand. A splice settles through the funnel (`settleSeparator` at the commit ceremony, `spliceChildrenSettled` under the path-addressed doors); the exemptions are head normalizations inside built subtrees, the positional rotation in `reorder.ts`, and the three sites whose rule a splice window cannot infer (the gap-caret mint, the same-block range-delete write, the empty-marker sublist separator), each named with its reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | L       | `test/invariants/lint/separator-write-doors.test.ts`                                                                                                   |
| G4.43 | Split-landing parity: every file naming `splitNode` reads `secondHalfIndex` and asserts the landing through `assertSplitLanding`, and carries at least one assertion per split CALL — so a caller growing a second split whose landing it re-derives fails too. G1.34's guard only fires where a caller calls it, and a site seating its caret at `i + 1` never crosses it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | L       | `test/invariants/lint/split-landing-parity.test.ts`                                                                                                    |
| G4.44 | Live ranged-edit surface parity: every editable PROSE surface (an editable-surface factory, its own `beforeinput`, and a read of the inline-construct policy table) resolves native ranged edits through `live-selection-edit.ts` :: `resolveLiveRangeEdit`, and no other file calls it. A destructive input carries the range it will rewrite on the EVENT (`getTargetRanges()`), not in the selection, so a surface reading only its live selection sees nothing at a collapsed caret: word, line and drag deletes reached the bytes natively and cut through delimiter runs the reader never saw. The fenced-code surface is outside the set for a reason the scan can see, its body holding no inline constructs to strand                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | L       | `lint/live-ranged-edit-parity.test.ts`                                                                                                                 |
| G4.45 | Settle-funnel caller census: the bare tree-op primitives (`splitNode`, `deleteNode`, the three merge doors) splice a body without settling it, so every file importing one is declared with the ceremony whose settle covers its writes, and each is asserted to reach a commit door or a settle entry. Keyed on the IMPORT, so an alias still enrolls its file and a same-named action-bundle method does not. The tail rule (GH #168) lives in the settle: a caller outside the ceremony leaves it unrun and the document one folded line short of its own reload, silently, since bytes still round-trip                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | L       | `lint/settle-funnel-callers.test.ts`                                                                                                                   |
| G4.46 | Ancestry fold-sink census: `rebuildUnsharedChain` / `rebuildUnsharedAncestry` take a required-nullable `folds` sink, and a fold splices the PARENT's children — so passing a sink is the claim "I can reconcile that scope's ids/refs" and `null` is an explicit decline. The type stops an omission but not caller N+1 answering `null` because reconciling was inconvenient, and a wrong id length is permanent. Every production call site is enumerated with the stance it takes and why, so a new caller fails the gate the day it is born                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | L       | `lint/ancestry-fold-sink-thread.test.ts`                                                                                                               |
| G4.47 | Whole-block editing-host reader census: every `[contenteditable]` selector read and every `document.activeElement` identity read routes through the host-aware predicate or declares its own answer with the reason. The hidden host is a real contenteditable that paints nothing, so a selector read excluding it or a focus comparison by identity answers for the wrong element while every byte still round-trips — a wrong hit-test endpoint, a tab stop with no input door. Two of the three spellings are scanned; dispatch-target identity is probed-benign and not enumerable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | L       | `lint/whole-block-host-readers.test.ts`                                                                                                                |
| G4.48 | Wall-clock budgets outside the perf projects go through the growth harness: an absolute `performance.now`/`Date.now` budget in a behavior suite is machine-speed-dependent and reds on a loaded host, where `measureScanGrowth`'s N-vs-4N ratio cancels the machine. Allowlisted residue carries its reason (the harness itself; a recursion-depth bound; an elapsed-time oracle a ratio cannot express)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | L       | `lint/wall-clock-budgets.test.ts`                                                                                                                      |
| G4.49 | E2E composition rides the shared IME driver: a spec constructing a `CompositionEvent` or an `insertCompositionText` input event by hand exercises a browser no user has, so hand-fired sequences fail the scan. The driver file is the one exemption (WebKit exposes no CDP session, so its arm has nowhere else to live), and the scan pins the hand-fired shape to that file EXACTLY, so a widened exemption and a deleted WebKit arm each fail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | L       | `e2e/lint/composition-driver.test.ts`                                                                                                                  |
| G4.50 | Cross-block-range census: every block command id either joins the range-declined set, joins the set with a cross-block arm, or is recorded range-safe with its reason. Membership is hand-maintained and the decline is the only thing keeping a rebind or the `runCommand` door from spending one block's offsets against a painted multi-block range, so an id classified nowhere fails the day it is minted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | L       | `lint/range-command-census.test.ts`                                                                                                                    |
| G4.51 | Debounced-checkpoint pairing: a file pushing a typing checkpoint also arms the pause window. The two halves sit at different points in the keystroke on purpose — the arm follows the settle — so a push with no arm leaves a batch no pause can end, every later keystroke joins it, and one Ctrl+Z unwinds the session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | L       | `lint/debounced-checkpoint-pairing.test.ts`                                                                                                            |
| G4.52 | Content-version door census: the version is ANNOUNCED at each door that writes the document bytes, never derived from a walk of the tree, so the announcements are a declared set and the shape an out-of-ceremony write has (unsharing a spine off the editor's own `deps.doc`) enrols its file. The commit ceremony covers every structural writer under it; the routine-typing writes, the history swap and the `source` prop swap answer for themselves. A silent door serves every whole-document memo a stale answer with nothing failing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | L       | `lint/content-version-doors.test.ts`                                                                                                                   |

| G4.53 | Descriptor field census: `BlockKindDescriptor` and the field reference table in `docs/design/plugin-contract.md` are one set, both directions, keyed on the field-name column alone so the prose columns stay rewritable. The registration shape freezes at 1.0, which makes the published table the inventory of what froze: a field landing undocumented and a row outliving its field are the two ways it stops being one. The manifest the census reads is complete by compile error (`schema/block-kind-descriptor.ts` :: `DESCRIPTOR_FIELDS`), so the scan never has to parse the type | L | `lint/descriptor-field-census.test.ts` |

| G4.54 | Published entry barrels are import sinks: no module in an entry's own import closure may import that entry back. Rollup assigns the two sides of such a re-export cycle to different chunks and warns that execution order will break, and only a consumer's bundler ever sees it, because in-repo `$lib` resolves to source and assigns no chunks at all. The entry list derives from package.json `exports`, so a new published subpath inherits the rule unasked | L | `lint/entry-barrel-sink.test.ts` |
| G4.55 | Docs name the package `@voithos-labs/aragonite`, never the bare `aragonite`, which belongs to an unrelated npm package. `docs/changelog/` is exempt: it records what shipped under the name of the day. | L | `lint/doc-package-name.test.ts` |
| G4.56 | Inline-tree and rendered-DOM walks are ITERATIVE: no function under `core/inline/`, `cursor/`, `ambient/` or `components/blocks/text/` that reads a node's `children`/`childNodes` may sit on a call cycle, its own included. Inline nesting depth is input-controlled (`**` nests one construct per pair), so a per-level frame overflows the stack and strands the block in the failed-block fallback, which cannot heal — the renderer and the offset walk took explicit stacks for exactly that reason and four walks one call later did not, which voided the protection (#200). The scan reads brace-matched bodies through the literal-aware walk, so a `{` or a call inside a string cannot move the boundary it reads. The scan resolves a call to EVERY declaration bearing that name and tracks reachability per declaration, so a file spelling two walkers alike cannot hide the recursive one behind the iterative one; a second assertion fails that repeat outright, which is what makes the exception map's `path :: name` key address one walk. The map is EMPTY by design: an entry is a stated overflow. The join seam's `clipNodes` REBUILDS a tree rather than reading one and still routes through the same pre-order, since a construct the cut crosses hands its place to its own clipped children (#226) | L | `lint/inline-walk-iterative.test.ts` |
| G4.57 | The source-scan lexer is held against TypeScript's: every character of each scanned `.ts` file and each `.svelte` script block is classified comment/string/template/regex/code by `spanAt` and by a `createSourceFile` + rescanning-scanner oracle, and the two must agree. Sixty-odd censuses read code through that lexer, so a literal it misreads shrinks their populations at once with nothing red, which is how a regex-blind walk and a `}`-opens-a-regex rule both shipped. The differential also pins `stripComments` to the same reading, so the guard cannot drift onto a lexer no census uses. TypeScript cannot lex markup: the `.svelte` markup half is pinned against a corpus instead, and three shapes there record a simplification rather than the truth | L | `lint/scan-source.differential.test.ts` |

**G4.4's allowlist** — the only sanctioned timing primitives; anything else trips the scan: rAF throttles in `selection/autoscroll.ts` (frame-paced autoscroll) and `selection/pointer-session.ts` (pointermove coalescing — the one home every drag lifecycle rides); the `setTimeout` wall-clock undo debounce in `editor-actions/commit/text-batch.ts` (a tick-grained microtask can't express "the user stopped typing"); and the `setTimeout` scan deadline in `search/regex-executor.ts` (a cancellation budget, not an ordering primitive: nothing awaits the timer).

**G4.7 generalizes the image-widget path-staleness incident.** A prose block's render is memoized on `renderKey` (`ambientPrefixText` + `raw` + the LRD signature epoch — a compact stamp that changes exactly when the signature string does — + image-load policy); the inline DOM it builds must depend on nothing outside that key. The incident: the image widget baked the paragraph's path into a `data-*` attribute and its click handler at build time, but the key omitted the path — so a block whose index shifted without a raw/ambient/link change never re-baked, and click-to-select resolved a stale node. Fixed by resolving the path live from the host's reactive `data-block-path` at event time. Two latent siblings closed the same way: `imageLoadPolicy` is now keyed when the block holds an image, and the task checkbox's `aria-checked` derives from the keyed `taskMarker` rather than the parallel `taskChecked` field.

The rule for any new memoized surface: **key on every input the built output reads, or never snapshot a live value into the output — resolve it live.** G4.2 is the one automated proxy (it keeps the non-reactive inline cache off the render path); the general contract is doc-enforced (`D`) with per-instance regression tests (`N`).

## Accessibility (WCAG 2.1 AA — axe ratchet)

Target: WCAG 2.1 AA, enforced by an `@axe-core/playwright` baseline-ratchet gate (`test:e2e:a11y`, part of `npm test`). axe runs over `.editor` across representative states (default content, active cross-block selection, failed-block fallback, blocked-scheme link) and fails on any violation whose rule id is not in the committed allowlist (`src/lib/e2e/a11y/axe-baseline.json`).

That allowlist is the executable, milestone-tied log of deferred AA work (contrast → CSS-ownership migration; per-block accessible names + the focusable thematic-break separator → 1.1) and **only shrinks**. The cross-block selection — overlay-painted with native selection suppressed, so otherwise invisible to assistive tech — is exposed through a visually-hidden `aria-live` region fed by the pure `createSelectionDescription` builder.

## Foundation

- `BLOCK_KIND_TABLE` / `ALL_BLOCK_KINDS` — union-derived kind manifest (`core/nodes.ts`).
- `assertInvariant` — the dev-runtime channel, decoupled from general dev warnings (`assert.ts`, beside `dev-warn.ts`: a dependency-free leaf every subsystem including `invariants/` reaches).
- A fire reds its owning unit test as well as the e2e specs: `devWarn` runs under the unit
  runner and reaches a structured sink the harness reads. No `invariant:` fire may be waived
  run-wide; a test that provokes one claims it locally (`docs/contributing/testing.md`).
- fast-check harness + the `test:editor:invariants` script.
