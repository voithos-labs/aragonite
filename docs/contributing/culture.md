# Engineering Culture (moved)

This file split in two, so an older pointer lands in one half or the other. Sorry about that, here
is where the halves went.

Every rule, and the section names most pointers cite (Fixing bugs, Testing shape, The bug shape to
fear, Records), now lives in [`rules.md`](rules.md): read that before your first edit. The eight incidents that bought
the rules, and the headings a pointer names when it cites one of them (offset translation has one
home, registries are code not state, only `await tick()` for sequencing), moved to
[`casebook.md`](casebook.md): read that before your first structural change.
